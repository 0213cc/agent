# 功能 4: 文献推荐系统 - 实现文档

## 📚 功能概述

为知识图谱中的每个概念提供相关学术文献推荐，帮助用户深入学习和研究。

## ✨ 核心特性

### 1. 学术论文推荐
- 集成 **Semantic Scholar API**（免费学术搜索引擎）
- 为每个概念推荐最相关的 5 篇学术论文
- 显示论文标题、作者、年份、摘要、引用次数
- 提供论文链接，可直接跳转查看

### 2. 智能展示
- 在节点详情面板中展示文献推荐
- 右键点击节点 → 同时显示维基百科和文献推荐
- 按引用次数和相关性排序
- 优雅的卡片式布局

### 3. 云原生特征
- **外部 API 集成**：Semantic Scholar 学术数据库
- **异步请求**：使用 aiohttp 进行非阻塞 HTTP 调用
- **容错处理**：网络失败时优雅降级
- **微服务架构**：独立的文献推荐服务端点

## 🏗️ 技术实现

### 后端实现

#### 1. API 端点
```python
@app.get("/api/literature/{concept}")
async def get_literature_recommendations(concept: str)
```

**功能**：
- 接收概念名称
- 调用 Semantic Scholar API 搜索相关论文
- 返回前 5 篇最相关的论文

**返回数据结构**：
```json
{
  "success": true,
  "data": {
    "papers": [
      {
        "title": "论文标题",
        "authors": ["作者1", "作者2"],
        "year": 2023,
        "abstract": "论文摘要...",
        "url": "https://...",
        "citations": 150,
        "venue": "会议/期刊名称"
      }
    ],
    "count": 5
  }
}
```

#### 2. Semantic Scholar API 集成
- **API 端点**：`https://api.semanticscholar.org/graph/v1/paper/search`
- **查询参数**：
  - `query`: 概念名称
  - `limit`: 返回数量（5篇）
  - `fields`: 需要的字段（标题、作者、年份、摘要、引用等）
- **无需 API Key**：免费使用，有速率限制

#### 3. 错误处理
- 网络超时：10秒超时限制
- API 失败：返回友好错误信息
- 无结果：提示"暂无相关文献"

### 前端实现

#### 1. 状态管理
```javascript
const [literatureData, setLiteratureData] = useState(null);
const [loadingLiterature, setLoadingLiterature] = useState(false);
```

#### 2. 数据获取函数
```javascript
const fetchLiterature = async (nodeName) => {
  // 调用后端 API
  // 处理响应数据
  // 更新状态
}
```

#### 3. UI 组件
- **文献列表**：卡片式布局
- **论文卡片**：
  - 标题（加粗）
  - 作者列表（斜体，最多显示3位）
  - 发表年份
  - 会议/期刊名称
  - 摘要（前300字）
  - 引用次数
  - 查看链接

#### 4. 交互设计
- 右键点击节点 → 打开详情面板
- 同时加载维基百科和文献推荐
- 滚动查看多篇论文
- 悬停效果：卡片高亮、左移动画

## 📊 数据流程

```
用户右键点击节点
    ↓
handleNodeInfo(nodeId)
    ↓
fetchWikipedia(nodeName) + fetchLiterature(nodeName)
    ↓
前端调用: GET /api/literature/{concept}
    ↓
后端调用: Semantic Scholar API
    ↓
解析论文数据
    ↓
返回前端展示
```

## 🎨 UI 设计

### 文献推荐面板
```
┌─────────────────────────────────────┐
│ 📚 相关文献推荐                      │
├─────────────────────────────────────┤
│ ┌─────────────────────────────────┐ │
│ │ 论文标题                         │ │
│ │ 作者1, 作者2 et al. (2023)      │ │
│ │ Conference/Journal Name         │ │
│ │ 摘要内容...                      │ │
│ │ 📊 引用次数: 150  [查看论文 →]  │ │
│ └─────────────────────────────────┘ │
│ ┌─────────────────────────────────┐ │
│ │ 另一篇论文...                    │ │
│ └─────────────────────────────────┘ │
└─────────────────────────────────────┘
```

### 样式特点
- **配色**：蓝色主题（#3498db）
- **动画**：悬停时左移 + 阴影
- **布局**：垂直卡片列表，间距 20px
- **滚动**：最大高度 500px，超出滚动

## 🔧 文件修改清单

### 后端
- ✅ `backend/api/main.py`
  - 添加 `/api/literature/{concept}` 端点
  - 集成 Semantic Scholar API
  - 异步 HTTP 请求处理

### 前端
- ✅ `frontend/src/App.jsx`
  - 添加 `literatureData` 和 `loadingLiterature` 状态
  - 添加 `fetchLiterature()` 函数
  - 修改 `handleNodeInfo()` 同时获取文献
  - 添加文献推荐 UI 组件

- ✅ `frontend/src/App.css`
  - 添加 `.literature-section` 样式
  - 添加 `.paper-item` 卡片样式
  - 添加悬停动画效果
  - 调整节点详情面板宽度（600px）

## 🌐 云原生特征体现

### 1. 外部服务集成
- **Semantic Scholar API**：学术数据库服务
- **RESTful API**：标准化接口调用
- **异步处理**：非阻塞 I/O 操作

### 2. 微服务架构
- **独立端点**：`/api/literature/{concept}`
- **单一职责**：专注于文献推荐
- **松耦合**：与其他功能独立

### 3. 容错设计
- **超时控制**：10秒超时限制
- **错误处理**：网络失败时优雅降级
- **用户反馈**：清晰的加载和错误提示

### 4. 可扩展性
- **易于替换**：可切换到其他学术 API（如 arXiv、PubMed）
- **缓存支持**：未来可添加 Redis 缓存
- **速率限制**：可添加请求限流

## 📈 性能优化

### 1. 并行请求
- 维基百科和文献推荐同时请求
- 减少用户等待时间

### 2. 数据限制
- 只返回前 5 篇论文
- 摘要截断到 300 字
- 作者列表最多显示 3 位

### 3. 前端优化
- 滚动容器（max-height: 500px）
- 懒加载（按需请求）
- 缓存状态（避免重复请求）

## 🧪 测试建议

### 1. 功能测试
- [ ] 右键点击节点，查看文献推荐
- [ ] 验证论文信息完整性（标题、作者、年份等）
- [ ] 点击"查看论文"链接，确认跳转正确
- [ ] 测试无结果情况（生僻概念）

### 2. 性能测试
- [ ] 测试 API 响应时间（应 < 3秒）
- [ ] 测试网络失败情况
- [ ] 测试并发请求（多个节点）

### 3. UI 测试
- [ ] 验证卡片布局和样式
- [ ] 测试悬停动画效果
- [ ] 测试滚动功能
- [ ] 测试移动端响应式

## 🎯 使用场景

### 场景 1：深入学习
用户生成"神经网络"知识图谱 → 右键点击"反向传播"节点 → 查看相关学术论文 → 深入学习算法原理

### 场景 2：学术研究
研究人员探索"量子纠缠"概念 → 查看最新研究论文 → 了解前沿进展和引用情况

### 场景 3：跨学科探索
学习"熵"概念 → 查看物理学、信息论、统计学等不同领域的论文 → 理解跨学科应用

## 📝 后续优化方向

### 1. 多数据源集成
- [ ] 添加 arXiv API（物理、数学、计算机）
- [ ] 添加 PubMed API（生物医学）
- [ ] 添加 Google Scholar（综合）

### 2. 智能推荐
- [ ] 基于用户浏览历史推荐
- [ ] 基于图谱结构推荐相关论文
- [ ] 使用 LLM 生成论文摘要

### 3. 缓存优化
- [ ] Redis 缓存热门概念的文献
- [ ] 设置合理的 TTL（24小时）
- [ ] 减少 API 调用次数

### 4. 用户交互
- [ ] 收藏论文功能
- [ ] 导出文献列表（BibTeX 格式）
- [ ] 论文评分和评论

## 🎉 功能亮点

1. **学术价值**：为跨学科学习提供权威文献支持
2. **用户体验**：一键获取相关论文，无需手动搜索
3. **数据质量**：Semantic Scholar 提供高质量学术数据
4. **云原生**：外部 API 集成，体现微服务架构
5. **可扩展**：易于添加更多学术数据源

---

**实现时间**：2026-01-20  
**状态**：✅ 已完成，等待测试  
**下一步**：用户测试 → 实现功能 5（用户评分系统）
